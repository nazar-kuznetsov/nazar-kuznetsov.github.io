/*

let footerS = new Slider({
	el: {
		container: '.slider-transform ul', // * Обезательно нужно передать родительный контейнер в котором дочерные элементы это слайды
		next: '.sliders__controls2 #sliders__next', // (не обезательынй параметр) указать селектрор для кнопки вперед (если добавлять кнопки то обезатель должны быть и вперед и назад)
		prev: '.sliders__controls2 #sliders__prev', // (не обезательынй параметр) указать селектрор для кнопки назад (если добавлять кнопки то обезатель должны быть и вперед и назад)
		pagination: '.sliders__controls2 .sliders__circle' // (не обезательынй параметр) это кнопки переключение обычно точки или картинки
	},
	//fade : false, // (не обезательынй параметр по умолчание false) если поставить true слайдер будет рабоать в стили fade тоесть через opacity
	//controlSpeed: false, // (не обезательынй параметр по умолчание false) следующий слайд появиться только после полного завершение анимации у текущего слайда
	//auto: true, // (не обезательынй параметр по умолчание false) автоматическая прокрутка если не установлено время то будет браться время по default 4сек
	//time: 1000, // (не обезательнй параметр по умолчание 4000ms тоесть 4сек) отвечает за сколько сеунд будет перелючаться слайдер в auto режиме
	//callback: function () {},// (не обезательнй параметр) установить callback который будет вызиваться каждый раз после перелистывание слайда
	//vartical: false, // (не обезательынй параметр по умолчание false) если установить true слайдер будет рабоать в вертикальном режиме одновремено можно использвать что-то одно либо fade 
})
*/
// если используеться слайдер горизонтальный или вертикальный то в стилях должен быть класс animate у которого будет transition 